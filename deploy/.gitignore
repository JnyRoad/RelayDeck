# =============================================================================
# RelayDeck Deploy Directory - Git Ignore
# =============================================================================

# Data directories (generated at runtime when using docker-compose.local.yml)
data/
postgres_data/
redis_data/

# Environment configuration (contains sensitive information)
.env

# Backup files (may contain runtime or database data)
backups/
*.backup
*.bak

# Temporary files
*.tmp
*.log
